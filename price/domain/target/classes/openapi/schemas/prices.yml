filter:
  type: object
  properties:
    selectedDate:
      type: string
      format : date-time
    productId:
      type: integer
    brandId:
      type: integer

priceResult:
  type: object
  properties:
    productId:
      type: integer
    brandId:
      type: integer
    priceList:
      type: integer
    startDate:
      type: string
      format : date-time
    endDate:
      type: string
      format : date-time
    price:
      type: number
      format : double

